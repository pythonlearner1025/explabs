import styles from './styles/menu.css';
import logo from './assets/favicon.ico';

function Menubar({selectedCharacter, onSelectCharacter}) {
  //<img src={null} alt="Logo" className={styles.logo} />
  return (
    <div class="menu">
        Menu
    </div>
  );
}

export default Menubar;
